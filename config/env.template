# OpenClaw Credentials Template
# Copy this to ~/.openclaw/.env and fill in your values
# IMPORTANT: chmod 600 ~/.openclaw/.env after creating

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Email password (if using email channel)
EMAIL_PASSWORD=your-email-password-here

# Gateway auth token - generate with: openssl rand -hex 32
# Must match the token in openclaw.json
OPENCLAW_GATEWAY_TOKEN=your-generated-token-here

# Docker volume paths (required by docker-compose.yml)
OPENCLAW_CONFIG_DIR=~/.openclaw
OPENCLAW_WORKSPACE_DIR=~/.openclaw/workspace
